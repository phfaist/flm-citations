flm:
  parsing:
    enable_comments: true
  features:
    flm_citations:
        sources:
          - $defaults:
          - $merge-config:
              name: 'bibliographyfile'
              config:
                cite_prefix: b
                bibliography_file:
                  - bibpreset.yaml
                  - anotherbibtest.json
