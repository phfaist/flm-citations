llm:
  parsing:
    enable_comments: true
  features:
    - $preset: defaults
    - name: 'llm_citations.feature_cite.FeatureCiteAuto'
      config:
        sources:
          - $preset: defaults
          - $preset: merge-config
            $name: 'bibliographyfile'
            $config:
              cite_prefix: b
              bibliography_file:
                - bibpreset.yaml
                - anotherbibtest.json
